export enum API_METHODS {
  ROOT = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  LOGOUT = '/logout',
  SINGLE_ITEM = '/:id',
  PODIUM_PLAYERS = '/podium',
  JOIN_ROOM = '/join_room',
  LEAVE_ROOM = '/leave_room',
  MOST_POPULAR_ROOMS = '/most_popular',
}
