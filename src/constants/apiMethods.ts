export enum API_METHODS {
  ROOT = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  LOGOUT = '/logout',
  SINGLE_ITEM = '/:id',
  TOP_PLAYERS = '/top',
  JOIN_ROOM = '/join_room',
  LEAVE_ROOM = '/leave_room',
  MOST_POPULAR_ROOMS = '/most_popular',
}
